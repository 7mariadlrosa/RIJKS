<div class="container-obras">
    <h2 class="title-collections">OBRAS DE LA COLECCIÓN</h2>
    <div class="row">
        {{#each artData}}
        <div class="col-md-4">
            <div class="card" style="width: 30rem">
                <img src={{webImage.url}} class="card-img-top" alt="...">
                <div class="card-body">
                    <h6>{{title}}</h6>
                    <h5>{{principalOrFirstMaker}}</h5>
                    <div>
                        <div>
                            <button type="button" class="btn btn-outline-warning"><a
                                    href="/collections/{{id}}">DETALLES</a></button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>